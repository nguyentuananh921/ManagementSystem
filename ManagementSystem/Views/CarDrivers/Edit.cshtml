@model ManagementSystem.Models.CarDriver

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>CarDriver</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="CarID" />
            <input type="hidden" asp-for="PeopleID" />
            <div class="form-group">
                <label asp-for="DriverNote" class="control-label"></label>
                <input asp-for="DriverNote" class="form-control" />
                <span asp-validation-for="DriverNote" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
